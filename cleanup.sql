-- ============================================================
-- LogiTrack — Mock Data Cleanup
-- Removes all mock data before handing over to the client
-- Run this in Supabase: SQL Editor → paste → Run
-- ============================================================

DELETE FROM expenses;
DELETE FROM trips;
